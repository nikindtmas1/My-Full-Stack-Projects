import * as api from '../api';

const getPosts = () => async (dispatch) => {

    try {
        
    } catch (error) {
        
    }


    const action = { type: 'FETCH_ALL', payload: [] }

    dispatch(action);
}